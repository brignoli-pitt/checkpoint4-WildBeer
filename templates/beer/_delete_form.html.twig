<form method="post" action="{{ path('app_beer_delete', {'id': beer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ beer.id) }}">
    <button class="btn">Delete</button>
</form>
